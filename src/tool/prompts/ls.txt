Lists files and directories in a given path.

Usage:
- Returns entries sorted alphabetically with directories first.
- Directories are indicated with a trailing '/'.
- Use this tool to explore the file system structure.